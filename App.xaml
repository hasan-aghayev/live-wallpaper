<Application x:Class="LiveWallpaper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LiveWallpaper"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- HaS Studio Tokens (from design.md) -->
            <Color x:Key="UiBgColor">#0A0D13</Color>
            <Color x:Key="UiSurfaceColor">#121821</Color>
            <Color x:Key="UiSurfaceRaisedColor">#18202B</Color>
            <Color x:Key="UiSurfaceInsetColor">#0D1219</Color>
            <Color x:Key="UiControlColor">#111722</Color>

            <SolidColorBrush x:Key="UiBgBrush" Color="{StaticResource UiBgColor}"/>
            <SolidColorBrush x:Key="UiSurfaceBrush" Color="{StaticResource UiSurfaceColor}"/>
            <SolidColorBrush x:Key="UiSurfaceRaisedBrush" Color="{StaticResource UiSurfaceRaisedColor}"/>
            <SolidColorBrush x:Key="UiSurfaceInsetBrush" Color="{StaticResource UiSurfaceInsetColor}"/>
            <SolidColorBrush x:Key="UiControlBrush" Color="{StaticResource UiControlColor}"/>

            <Color x:Key="UiBorderColor">#222B38</Color>
            <Color x:Key="UiBorderStrongColor">#52C4F0</Color>
            <SolidColorBrush x:Key="UiBorderBrush" Color="{StaticResource UiBorderColor}"/>
            <SolidColorBrush x:Key="UiBorderStrongBrush" Color="{StaticResource UiBorderStrongColor}"/>

            <Color x:Key="UiAccentColor">#19AFE7</Color>
            <Color x:Key="UiAccentStrongColor">#0E92CE</Color>
            <Color x:Key="UiAccentSoftColor">#2419AFE7</Color>
            <Color x:Key="UiTextOnAccentColor">#000000</Color>
            <SolidColorBrush x:Key="UiAccentBrush" Color="{StaticResource UiAccentColor}"/>
            <SolidColorBrush x:Key="UiAccentStrongBrush" Color="{StaticResource UiAccentStrongColor}"/>
            <SolidColorBrush x:Key="UiAccentSoftBrush" Color="{StaticResource UiAccentSoftColor}"/>
            <SolidColorBrush x:Key="UiTextOnAccentBrush" Color="{StaticResource UiTextOnAccentColor}"/>

            <Color x:Key="UiTextColor">#EDF4FB</Color>
            <Color x:Key="UiTextSecondaryColor">#A5B3C5</Color>
            <Color x:Key="UiTextMutedColor">#718096</Color>
            <SolidColorBrush x:Key="UiTextBrush" Color="{StaticResource UiTextColor}"/>
            <SolidColorBrush x:Key="UiTextSecondaryBrush" Color="{StaticResource UiTextSecondaryColor}"/>
            <SolidColorBrush x:Key="UiTextMutedBrush" Color="{StaticResource UiTextMutedColor}"/>

            <Color x:Key="UiDangerColor">#FB7185</Color>
            <Color x:Key="UiWarningColor">#FBBF24</Color>
            <Color x:Key="UiSuccessColor">#34D399</Color>
            <SolidColorBrush x:Key="UiDangerBrush" Color="{StaticResource UiDangerColor}"/>
            <SolidColorBrush x:Key="UiWarningBrush" Color="{StaticResource UiWarningColor}"/>
            <SolidColorBrush x:Key="UiSuccessBrush" Color="{StaticResource UiSuccessColor}"/>

            <CornerRadius x:Key="UiRadiusXs">5</CornerRadius>
            <CornerRadius x:Key="UiRadiusSm">7</CornerRadius>
            <CornerRadius x:Key="UiRadiusMd">8</CornerRadius>
            <CornerRadius x:Key="UiRadiusLg">10</CornerRadius>
            <CornerRadius x:Key="UiRadiusPanel">14</CornerRadius>

            <FontFamily x:Key="UiFont">Segoe UI, Poppins, -apple-system, sans-serif</FontFamily>
            <FontFamily x:Key="UiFontMono">JetBrains Mono, Segoe UI Mono, Consolas, monospace</FontFamily>

            <!-- Buttons -->
            <Style x:Key="UiButtonPrimary" TargetType="Button">
                <Setter Property="Height" Value="40"/>
                <Setter Property="Background" Value="{StaticResource UiAccentBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource UiTextOnAccentBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="20,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiAccentBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="{StaticResource UiRadiusSm}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiAccentStrongBrush}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource UiAccentStrongBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="UiButtonSecondary" TargetType="Button">
                <Setter Property="Height" Value="38"/>
                <Setter Property="Background" Value="{StaticResource UiSurfaceRaisedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource UiTextSecondaryBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="18,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="{StaticResource UiRadiusSm}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiAccentSoftBrush}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource UiBorderStrongBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="UiButtonDanger" TargetType="Button">
                <Setter Property="Height" Value="38"/>
                <Setter Property="Background" Value="{StaticResource UiSurfaceRaisedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource UiDangerBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="18,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="{StaticResource UiRadiusSm}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiDangerBrush}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource UiDangerBrush}"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="UiChipButton" TargetType="Button">
                <Setter Property="Height" Value="32"/>
                <Setter Property="Background" Value="{StaticResource UiSurfaceInsetBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource UiTextSecondaryBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="12,0"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="{StaticResource UiRadiusXs}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiAccentSoftBrush}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource UiBorderStrongBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Compact, consistent dark checkbox -->
            <Style x:Key="UiCheckBox" TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="box" Width="18" Height="18" CornerRadius="5"
                                        Background="{StaticResource UiControlBrush}"
                                        BorderBrush="{StaticResource UiBorderBrush}" BorderThickness="1"
                                        VerticalAlignment="Center">
                                    <Path x:Name="check" Data="M 3,9 L 7,13 L 15,4" Stroke="{StaticResource UiTextOnAccentBrush}"
                                          StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          Visibility="Collapsed" Stretch="Fill" Margin="2"/>
                                </Border>
                                <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="box" Property="Background" Value="{StaticResource UiAccentBrush}"/>
                                    <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource UiAccentBrush}"/>
                                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource UiBorderStrongBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="box" Property="Opacity" Value="0.4"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ComboBox Styles -->
            <ControlTemplate x:Key="UiComboBoxToggleButtonTemplate" TargetType="ToggleButton">
                <Border x:Name="templateRoot" 
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="{StaticResource UiRadiusSm}">
                    <Border Width="24" HorizontalAlignment="Right">
                        <Path x:Name="arrow" 
                              Data="M 0 0 L 4 4 L 8 0 Z" 
                              Fill="{StaticResource UiTextMutedBrush}" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                    </Border>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource UiBorderStrongBrush}"/>
                        <Setter TargetName="arrow" Property="Fill" Value="{StaticResource UiTextBrush}"/>
                    </Trigger>
                    <Trigger Property="IsChecked" Value="true">
                        <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource UiAccentBrush}"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style x:Key="UiComboBoxItem" TargetType="ComboBoxItem">
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                    Padding="{TemplateBinding Padding}" 
                                    CornerRadius="{StaticResource UiRadiusXs}" Margin="2,1">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiAccentSoftBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource UiAccentBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource UiTextOnAccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="UiComboBox" TargetType="ComboBox">
                <Setter Property="Height" Value="38"/>
                <Setter Property="Background" Value="{StaticResource UiControlBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource UiTextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="14,0,34,0"/>
                <Setter Property="ItemContainerStyle" Value="{StaticResource UiComboBoxItem}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                                <ToggleButton x:Name="toggleButton" 
                                              Background="{TemplateBinding Background}" 
                                              BorderBrush="{TemplateBinding BorderBrush}" 
                                              BorderThickness="{TemplateBinding BorderThickness}" 
                                              Template="{StaticResource UiComboBoxToggleButtonTemplate}" 
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ContentPresenter x:Name="contentPresenter" 
                                                  Content="{TemplateBinding SelectionBoxItem}" 
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                                                  HorizontalAlignment="Left" 
                                                  VerticalAlignment="Center" 
                                                  Margin="{TemplateBinding Padding}" 
                                                  IsHitTestVisible="false"/>
                                <Popup x:Name="PART_Popup" 
                                       AllowsTransparency="true" 
                                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                       Placement="Bottom" 
                                       PopupAnimation="Fade">
                                    <Border x:Name="dropDownBorder" 
                                            MinWidth="{Binding ActualWidth, ElementName=templateRoot}" 
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                            Background="{StaticResource UiSurfaceRaisedBrush}" 
                                            BorderBrush="{StaticResource UiBorderBrush}" 
                                            BorderThickness="1" 
                                            CornerRadius="{StaticResource UiRadiusSm}" 
                                            Margin="0,4,0,0" 
                                            Padding="3">
                                        <ScrollViewer x:Name="DropDownScrollViewer">
                                            <ItemsPresenter x:Name="ItemsPresenter" 
                                                            KeyboardNavigation.DirectionalNavigation="Contained" 
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="toggleButton" Property="BorderBrush" Value="{StaticResource UiBorderStrongBrush}"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="true">
                                    <Setter TargetName="toggleButton" Property="BorderBrush" Value="{StaticResource UiAccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Cards -->
            <Style x:Key="UiCard" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource UiSurfaceRaisedBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource UiRadiusLg}"/>
                <Setter Property="Padding" Value="20"/>
            </Style>

            <Style x:Key="UiCardInset" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource UiSurfaceInsetBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource UiBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource UiRadiusMd}"/>
                <Setter Property="Padding" Value="16"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
